{
  "name": "server",
  "version": "1.0.0",
  "description": "mock server for flow steps hook",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "philippe",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "latest",
    "pg": "^7.10.0"
  }
}
